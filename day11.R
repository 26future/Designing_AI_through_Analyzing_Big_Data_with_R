#상관계수
#1. 연속형&연속형(모수적): 피어슨
#   -두 변수가 모두 정규분포를 따른다는 가정
#   - -1~1, 0에 가까우면 무상관, 1/-1에 가까우면 상관
#2. 연속형&연속형(비모수적), 순수형도 가능: 켄달 (순위), 스피어만 (순위)
#   -두 변수가 정규성을 따르지 ㅇ낳는 경우
#   -스피어만(켄달) 상관계수는 순위를 매겨서 상관계수를 구함

# 켄달 상관계수: 두 변수의 순위 비교 => 연관성
#켄달 상관계수= C-D/C+D
#               C: concordant pair, D: conconrdant pair가 아닌 수
#concordant pair: 

#R 웹 스크래핑= rvest
install.packages("rvest")
